<%= form_with(model: workout) do |form| %>
  <% if workout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(workout.errors.count, "error") %> prohibited this workout from being saved:</h2>

      <ul>
        <% workout.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

 <div class="field">
    <%= form.text_field :day, class: "form-control", placeholder:"Date (MM-DD-YYYY)" %>
  </div>
  <br>

 <div class="field">
    <%= form.text_field :workout_name, class: "form-control", placeholder:"Subject" %>
  </div>
    <br>
      <div class="field">
    <%= form.text_field :workout_type, class: "form-control", placeholder:"Category" %>
  </div>
    <br>
      <div class="field">
    <%= form.text_field :location, class: "form-control", placeholder:"Location" %>
  </div>
    <br>
      <div class="field">
    <%= form.text_field :time, class: "form-control", placeholder:"Duration (min)" %>
  </div>
    <br>
      <div class="field">
    <%= form.text_field :intensity, class: "form-control", placeholder:"Intensity (1-10)" %>
  </div>
    <br>

  <div class="actions">
    <%= form.submit class:"btn btn-primary" %>
  </div>
<% end %>

